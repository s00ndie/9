def difference(*nums):
    if len(nums) ==0:
        result = 0
        print(result)
    elif len(nums) >1:
        n1 = min(nums)
        n2 = max(nums)
        result = n2 - n1
        if type(result) is float:
            result = round(result,2)
            print(result)
        print(result)
